spring:
  application:
    name: ${APPLICATION_NAME:AuthService}
  datasource:
    url: jdbc:postgresql://localhost:5432/mental_health
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:santiagomac}
    password: ${DB_PASSWORD:santim100}
  jpa:
    hibernate:
      ddl-auto: update

security:
  jwt:
    secret-key: ${SECRET_KEY:un-secreto-largo-y-aleatorio-de-al-menos-32-bytes}